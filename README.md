# Button App

A minimal Flutter application with one button. Pressing the button intentionally has no effect.

## Run

After installing the Flutter SDK, run:

```powershell
flutter pub get
flutter run
```
